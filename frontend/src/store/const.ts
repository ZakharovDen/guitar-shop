export enum APIRoute {
  Products = '/products',
  Login = '/users/login',
  Logout = 'users/logout',
  CheckAuth = 'users/check',
}

export enum NameSpace {
  User = 'USER',
  Products = 'PRODUCTS',
}

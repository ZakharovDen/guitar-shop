export enum APIRoute {
  Products = '/products',
  Login = '/users/login',
  Register = '/users',
  CheckAuth = '/users/check',
  Logout = 'users/logout',
}

export enum NameSpace {
  User = 'USER',
  Products = 'PRODUCTS',
}
